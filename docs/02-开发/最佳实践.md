# 最佳实践

> **版本:** V4.0
> **更新时间:** 2026-06-04

本文档总结哈吉咪养成计划项目的开发和部署最佳实践。

---

## 📋 开发最佳实践

### 代码规范

#### TypeScript 规范

```typescript
// ✅ 好的做法
interface Cat {
  id: string
  name: string
  age: number
}

// ❌ 不好的做法
interface cat {
  Id: string
  Name: any
}

// ✅ 使用类型别名
type CatId = string
type Weight = number

// ✅ 使用联合类型
type Gender = 'male' | 'female'

// ✅ 使用可选属性
interface Cat {
  breed?: string // 可选
}
```

#### Vue 3 组件规范

```vue
<!-- ✅ 好的做法 -->
<script setup lang="ts">
import { ref, computed } from 'vue'

interface Props {
  catId: string
  title?: string
}

const props = withDefaults(defineProps<Props>(), {
  title: '默认标题'
})

const emit = defineEmits<{
  'update:modelValue': [value: string]
  'change': [event: Event]
}>()

// 响应式数据使用 ref
const loading = ref(false)

// 计算属性
const displayTitle = computed(() => {
  return props.title || '猫咪'
})
</script>

<!-- ❌ 不好的做法 -->
<script>
export default {
  data() {
    return {
      loading: false
    }
  }
}
</script>
```

### 文件组织

```
src/
├── components/
│   ├── cat/              # 按功能分组
│   │   ├── CatCard.vue
│   │   └── CatForm.vue
│   ├── common/           # 通用组件
│   │   ├── Button.vue
│   │   └── Input.vue
│   └── mascot/           # 吉祥物组件
│       └── MascotCharacter.vue
├── composables/          # 可复用逻辑
├── stores/               # Pinia stores
├── api/                  # API 调用
├── types/                # TypeScript 类型
└── utils/                # 工具函数
```

### 状态管理

```typescript
// ✅ 使用 Pinia 组合式 API
import { defineStore } from 'pinia'

export const useCatStore = defineStore('cat', () => {
  // state
  const cats = ref<Cat[]>([])

  // getters
  const activeCats = computed(() =>
    cats.value.filter(cat => cat.isActive)
  )

  // actions
  async function fetchCats() {
    const response = await getCats()
    cats.value = response.data
  }

  return { cats, activeCats, fetchCats }
})

// ❌ 避免在组件中直接修改 store
// ✅ 使用 store 的 actions
```

---

## 🔧 后端开发最佳实践

### 控制器规范

```typescript
// ✅ 好的做法
export async function getCats(req: Request, res: Response, next: NextFunction) {
  try {
    const userId = (req as any).user?.userId

    if (!userId) {
      return res.status(401).json(errorResponse('未授权'))
    }

    const cats = await catService.getCatsByUser(userId)
    res.json(successResponse(cats, '获取猫咪列表成功'))
  } catch (error) {
    next(error)
  }
}

// ❌ 不好的做法
export function getCats(req: any, res: any) {
  const cats = await prisma.cat.findMany()
  res.json(cats)
}
```

### 服务层设计

```typescript
// ✅ 业务逻辑在服务层
export async function createCat(data: CreateCatDto, userId: string) {
  // 1. 验证输入
  if (!data.name || data.name.trim().length === 0) {
    throw new Error('猫咪名称不能为空')
  }

  // 2. 检查业务规则
  const existingCats = await prisma.cat.count({ where: { userId } })
  if (existingCats >= 10) {
    throw new Error('最多只能添加10只猫咪')
  }

  // 3. 执行业务逻辑
  const cat = await prisma.cat.create({
    data: {
      ...data,
      userId
    }
  })

  return cat
}
```

### 错误处理

```typescript
// ✅ 统一错误处理
export class AppError extends Error {
  constructor(
    public statusCode: number,
    public message: string,
    public code?: string
  ) {
    super(message)
  }
}

// 使用
throw new AppError(404, '猫咪不存在', 'CAT_NOT_FOUND')

// 错误中间件
export function errorHandler(err: Error, req: Request, res: Response, next: NextFunction) {
  if (err instanceof AppError) {
    return res.status(err.statusCode).json(errorResponse(err.message, err.code))
  }

  console.error('[Error]', err)
  res.status(500).json(errorResponse('服务器内部错误'))
}
```

---

## 🚀 Git 工作流

### 分支策略

```
master          # 主分支，生产环境
  ├── develop   # 开发分支
      ├── feature/cats-api      # 功能分支
      ├── feature/weight-chart  # 功能分支
      └── fix/auth-bug          # 修复分支
```

### 提交规范

```bash
# 格式
<type>(<scope>): <subject>

# 类型
feat:     新功能
fix:      修复 bug
docs:     文档更新
style:    代码格式调整
refactor: 重构
test:     测试相关
chore:    构建/工具相关

# 示例
git commit -m "feat(cat): 添加多猫对比功能"
git commit -m "fix(auth): 修复 token 过期问题"
git commit -m "docs(readme): 更新安装说明"
```

### Code Review

检查清单：

- [ ] 代码符合项目规范
- [ ] 功能完整且测试通过
- [ ] 没有调试代码和 console.log
- [ ] 添加了必要的注释
- [ ] 更新了相关文档

---

## 🎨 UI/UX 最佳实践

### 组件设计

```vue
<!-- ✅ 好的做法 -->
<template>
  <div class="cat-card">
    <div class="cat-header">
      <img :src="cat.avatar" :alt="cat.name" />
      <h3>{{ cat.name }}</h3>
    </div>

    <div class="cat-body">
      <p>品种：{{ cat.breed || '未知' }}</p>
      <p>年龄：{{ cat.ageFormatted }}</p>
    </div>

    <div class="cat-actions">
      <button @click="handleView">查看</button>
      <button @click="handleEdit">编辑</button>
    </div>
  </div>
</template>

<script setup lang="ts">
interface Props {
  cat: Cat
}

const props = defineProps<Props>()
const emit = defineEmits<{
  view: [cat: Cat]
  edit: [cat: Cat]
}>()

function handleView() {
  emit('view', props.cat)
}

function handleEdit() {
  emit('edit', props.cat)
}
</script>
```

### 响应式设计

```css
/* ✅ 使用相对单位 */
.container {
  width: 100%;
  max-width: 1200px;
  padding: 0 20px;
}

/* ✅ 使用 Flexbox/Grid */
.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 20px;
}

/* ✅ 响应式图片 */
img {
  max-width: 100%;
  height: auto;
}
```

---

## 🔐 安全最佳实践

### 输入验证

```typescript
// ✅ 使用 express-validator
import { body, validationResult } from 'express-validator'

export const createCatValidation = [
  body('name')
    .trim()
    .notEmpty().withMessage('猫咪名称不能为空')
    .isLength({ max: 50 }).withMessage('名称不能超过50个字符'),
  body('breed')
    .optional()
    .isLength({ max: 50 }).withMessage('品种名称不能超过50个字符')
]

export function validate(req: Request, res: Response, next: NextFunction) {
  const errors = validationResult(req)
  if (!errors.isEmpty()) {
    return res.status(400).json(errorResponse('输入数据验证失败'))
  }
  next()
}
```

### 认证授权

```typescript
// ✅ 使用 JWT
export function authenticate(req: Request, res: Response, next: NextFunction) {
  const authHeader = req.headers.authorization

  if (!authHeader) {
    return res.status(401).json(errorResponse('未提供认证令牌'))
  }

  const token = authHeader.split(' ')[1]

  try {
    const decoded = jwt.verify(token, process.env.JWT_SECRET!)
    req.user = decoded
    next()
  } catch (error) {
    return res.status(401).json(errorResponse('认证令牌无效'))
  }
}
```

---

## 📊 性能优化最佳实践

### 前端优化

```typescript
// ✅ 路由懒加载
const Dashboard = () => import('./views/Dashboard.vue')

// ✅ 组件懒加载
<script setup>
import { defineAsyncComponent } from 'vue'

const HeavyComponent = defineAsyncComponent(() =>
  import('./HeavyComponent.vue')
)
</script>

// ✅ 列表虚拟化
import { useVirtualList } from '@vueuse/core'

const { list, containerProps, wrapperProps } = useVirtualList(
  sourceData,
  { itemHeight: 50 }
)
```

### 后端优化

```typescript
// ✅ 数据库查询优化
// 只选择需要的字段
const cats = await prisma.cat.findMany({
  select: {
    id: true,
    name: true,
    // 不选择不需要的字段
  }
})

// ✅ 使用索引
// schema.prisma
model Cat {
  // ...
  @@index([userId, isActive])
}

// ✅ 批量操作
const cats = await prisma.cat.createMany({
  data: catDataArray
})
```

---

## 🧪 测试最佳实践

### 单元测试

```typescript
// ✅ 使用 Vitest
import { describe, it, expect } from 'vitest'
import { formatAge } from '@/utils/age'

describe('formatAge', () => {
  it('应该正确格式化幼猫年龄', () => {
    const birthDate = new Date('2023-01-01')
    const currentDate = new Date('2023-04-01')
    const result = formatAge(birthDate, currentDate)

    expect(result).toBe('3个月')
  })

  it('应该处理空值', () => {
    const result = formatAge(null, new Date())
    expect(result).toBe('未知')
  })
})
```

### 集成测试

```typescript
// ✅ API 测试
import { describe, it, expect } from 'vitest'
import { request } from './setup'

describe('POST /api/cats', () => {
  it('应该创建新猫咪', async () => {
    const response = await request
      .post('/api/cats')
      .set('Authorization', `Bearer ${token}`)
      .send({
        name: '测试猫咪',
        gender: 'male',
        birthDate: '2023-01-01'
      })

    expect(response.status).toBe(201)
    expect(response.body.success).toBe(true)
    expect(response.body.data.name).toBe('测试猫咪')
  })
})
```

---

## 📝 文档最佳实践

### 代码注释

```typescript
/**
 * 计算猫咪的年龄
 * @param birthDate 出生日期
 * @param currentDate 当前日期（可选，默认为今天）
 * @returns 格式化的年龄字符串
 * @example
 * calculateAge(new Date('2023-01-01'))
 * // 返回: '1岁2个月'
 */
export function calculateAge(
  birthDate: Date,
  currentDate: Date = new Date()
): string {
  // 实现代码
}
```

### README 文档

```markdown
# 项目名称

简短描述项目

## 快速开始

\`\`\`bash
npm install
npm run dev
\`\`\`

## 功能特性

- 特性1
- 特性2

## 技术栈

- Vue 3
- Express
```

---

## 🔄 CI/CD 最佳实践

### GitHub Actions

```yaml
# ✅ 使用 GitHub Actions
name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build
```

---

## 📞 获取帮助

- 查看 [开发文档](./开发文档.md)
- 提交 [Issue](https://github.com/Tian-Zhen-Yin/PangHu/issues)
- 联系开发团队

---

_最佳实践文档最后更新：2026-06-04_
