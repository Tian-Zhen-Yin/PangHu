from zhipuai import ZhipuAI

client = ZhipuAI(api_key="054f0cb0bc2342ef903c443bc43ef2eb.Hp0ii26Tf855PoD5")

# 模拟一个会触发工具调用的场景
response = client.chat.completions.create(
    model="glm-4.7",
    messages=[{"role": "user", "content": "帮我查一下当前目录下的文件"}],
    tools=[{"type": "function", "function": {"name": "ls", "description": "list files"}}]
)

tool_call = response.choices[0].message.tool_calls[0]
print(f"GLM 字段名: {dir(tool_call)}") 
# 重点看这里是否有 'id'，还是只有 'tool_call_id'