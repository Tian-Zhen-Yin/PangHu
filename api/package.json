{
  "name": "panghu-api",
  "private": true,
  "dependencies": {
    "@prisma/client": "^6.19.2",
    "@prisma/adapter-pg": "^6.19.2",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "prisma": "^6.19.2"
  },
  "scripts": {
    "postinstall": "npx prisma generate --schema=../backend/prisma/schema.prisma --no-engine --no-hints || true"
  }
}
